{% include 'default/_form.html.twig' with {
    page_title: 'title.event_create',
    attr: {
        method: 'POST',
        action: url('event_create')
    },
    submit_label: 'form.action.create'|trans,
    back_to_list_path: 'event_index',
} %}
